name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  lint-typecheck:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @mcpspend/api run db:generate

      - name: Typecheck API
        run: pnpm --filter @mcpspend/api run typecheck

      - name: Typecheck dashboard
        run: pnpm --filter @mcpspend/dashboard run typecheck

      - name: Typecheck proxy
        run: pnpm --filter @mcpspend/proxy run typecheck

      - name: Run proxy unit tests
        run: pnpm --filter @mcpspend/proxy run test

      # The Stripe account is shared with three other products; this suite is
      # what keeps their subscriptions from being treated as ours.
      - name: Run API unit tests
        run: pnpm --filter @mcpspend/api run test

      # The blog loader is what broke every production build from 2026-07-14:
      # a pnpm override forced js-yaml 4 into gray-matter, which calls the
      # removed `yaml.safeLoad`. Nothing tested it, so the only thing that ever
      # ran it was the production build itself.
      - name: Run dashboard unit tests
        run: pnpm --filter @mcpspend/dashboard run test

  build-api:
    name: Build API
    runs-on: ubuntu-latest
    needs: lint-typecheck
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build API Docker image
        run: |
          docker build -f apps/api/Dockerfile -t mcpspend-api:ci .
          docker build -f apps/api/Dockerfile.worker -t mcpspend-worker:ci .

  build-dashboard:
    name: Build Dashboard
    runs-on: ubuntu-latest
    needs: lint-typecheck
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Dashboard Docker image
        run: docker build -f apps/dashboard/Dockerfile -t mcpspend-dashboard:ci .
